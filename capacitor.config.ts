import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.lojacadeira',
  appName: 'lojaVirtual',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
